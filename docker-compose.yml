services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./app_data:/app/app_data
    environment:
      - APP_DATA_DIR=/app/app_data

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
